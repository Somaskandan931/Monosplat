# Minimal dependencies for Colab GPU training.
# torch/torchvision are pre-installed on Colab — do NOT re-pin them here.

numpy>=1.24.0,<2.0.0
Pillow>=9.5.0
PyYAML>=6.0
tqdm>=4.65.0
scipy>=1.10.0
plyfile>=0.8.1
lpips>=0.1.4

# diff-gaussian-rasterization and simple-knn are installed via submodule in
# the Colab notebook — NOT via pip from PyPI. The notebook clones
# graphdeco-inria/gaussian-splatting and runs:
#
#   !pip install submodules/diff-gaussian-rasterization
#   !pip install submodules/simple-knn
#
# gsplat has been REMOVED — this project now uses diff-gaussian-rasterization
# (the original 3DGS CUDA kernel) for reliable gradient flow and densification.
